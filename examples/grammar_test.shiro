use std.io as io;

# Simple example test file

[# Nested [# 

multiline comments 

#] #]

let x = 69.0; # Mixed and [# Nested [# comments #] #]
let y = 16;
x = y == 2 * 3 + 1 - y;
let z = 32 * 4 + x;
x = z;
let a = true;
let b = false;
b = null;

let condition = (let x = 3) != 0;
let str = 'Hello, world!';

print(x);

x;
y;
z;

let obj = { 
    a = 23, 
    b = { c = 'Hello' },
    d = ['a', 'b', 'c', 32]
};

let arr_idx = obj.d[0];


if arr_idx == 'a' {
    print('If statement');
} else if (arr_idx == 'b') {
    print('Elseif statement');
} else {
    print('Else statement');

    let nested_obj = { a = 23 };
}

while arr_idx {
    print('test');
}

for let i = 0; i < 10; i += 1 {
    ;
}

fun test_function(a, b, c) {
    ;
}

let test_fun = fun () {
    return 23;
};

#['test', 1, 2, 3]

